# 6.4.8 · SOUND CHECK

Fabric speech service repair.

Linux systemd user services have a smaller PATH than an interactive Homebrew shell. The Unified Node now searches canonical Linuxbrew locations when resolving local effect binaries, so `fcl-node speak` and `lk fabric speak` use the same eSpeak NG + SoX installation that LOOK Games already sees.
