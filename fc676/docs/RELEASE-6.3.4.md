# Future Crash + LOOK 6.3.4 — Saved Means Saved

Albert Saved is now a real durable shelf rather than a dead rendering of live-fold controls. Clicking a saved item rehydrates a fresh live fold on the paper and scrolls to it; removing a saved item affects only the durable copy. Media folds, including audio visualizers, are rebuilt normally when restored.
