# Future Crash + LOOK 6.3.6 — Clean Feed

- `lk media arts` now targets the direct Classic Arts Showcase HLS feed in the native media player.
- Albert video folds are contain-fit and expandable. HLS is used directly where supported; otherwise the official CAS page is scaled as a browser fallback.
- The website fallback remains available rather than pretending the external stream is guaranteed forever.
