# 6.5.0 · LAST MILE

6.5 makes browser endpoints first-class effect endpoints rather than merely addressable browser sessions. The endpoint runtime now acknowledges effect state, waits for Safari audio unlock when necessary, and drains pending speech after a real user gesture. Albert and Signal use the same runtime contract.
