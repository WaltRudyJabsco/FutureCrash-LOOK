# 3.3.8 — Cheap Failure Enforcement

- Fix literal `\n` rendering in deterministic browser-open responses.
- Bare dropped/typed resource paths attach context and wait for a task.
- Shell filesystem discovery is blocked at the command boundary.
- File discovery must use LOOK `search_files`.
- Broad or weak resolution should ask the human for one useful clue rather than grind.
