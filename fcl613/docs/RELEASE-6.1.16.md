# 6.1.16 — Name Tag

Release names are presentation metadata, never part of machine version parsing. `fcl-node --version` remains human-readable; `fcl-node --version-number` returns only the numeric release and is used by the installer verifier.
